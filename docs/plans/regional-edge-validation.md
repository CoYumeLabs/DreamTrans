# 区域 Edge 与蓝绿发布验证记录

状态：开发验证中，尚未满足生产发布验收；不能将本记录视为上线批准。现有生产目录、卷、Tunnel 和 DNS 未操作。

## 2026-09-19 本地验证

- Node 24.18.0，已执行 `npm ci` 和 Playwright Chromium 安装。
- 后端 Go 1.26.5：`go mod tidy -diff`、依赖下载及 `go mod verify` 通过。
- golangci-lint 2.12.2，`--build-tags=event_worker`：0 issues。
- 迁移包连续到 054；全量迁移已应用到独立测试容器 `dreamtrans-edge-ci-pg` 的 `dreamtrans_edge_test` 数据库。
- 安装器 fresh lifecycle、administrator update 和 mocked backup 检查通过。
- 新发布控制器 7 项恢复/资源检查测试通过：有会话时超时保留、未确认的停止队列拒绝复用、先记录排空再停止、首次转换复用准确实例、拒绝错误数据目录、非 TTY 进度输出、主站与小规格 Edge 独立内存门槛。
- 完整后端 `go test -v -race -coverprofile=... ./...` 通过；独立 `event_worker` race 测试通过。
- 首次完整前端 `verify:ci` 命令成功结束：57 项 E2E 首轮通过、1 项 onboarding 重试通过（58 项总计）。失败 trace 显示模块加载 `ERR_NETWORK_CHANGED`；将等 Docker 构建/生命周期结束后完整重跑，不把这次结果描述为无不稳定测试。
- 在所有 Docker 构建与生命周期检查结束后，重新执行完整 `verify:ci`：59 项 E2E 全部首轮通过（含新增节点管理测试），无失败和重试。lint、type-check、build、core、long-session、AI、admin 验证全部通过。
- 主站与 Edge 镜像构建通过；Edge 镜像非 root、无数据库工具/主站 rag.db、拒绝 DATABASE_URL 凭证检查通过。
- 主站新镜像应用卷权限迁移、独立前端镜像构建与三入口运行时、生产镜像内文件提取 fixtures 检查通过；迁移后及标准部署升级/故障恢复集成测试通过，实际卷标识与新增数据保留。
- 原有 provider model、model catalog constraints、acquisition 迁移回放测试通过（隔离测试库）。

## 仍须完成的验收

1. 发布不同阶段中断恢复、真实长转录期间切换及异常回切的完整集成测试。
2. Edge 安装器不同中断阶段恢复；轮换/吊销及故障场景的完整管理员操作 E2E。
3. 长时间、多次故障及不同音频帧尺寸下的恢复边界；v2 首轮持久化确认与强制宕机验收见下文。
4. 跨节点恢复时供应商近似词时间戳导致的边界字幕重叠；首轮真实接管及重复收费检查见下文。
5. 指标完整性、受限凭证轮换与回传冲突的人工对账流程。
6. YuAction 现有生产安装的自动网络交接及 AI/知识库有供应商凭证时的端到端验收；固定入口与 R2 实网恢复验证已通过，见后文。
7. 缺少 Docker/Compose 的全新系统依赖安装分支、各安装中断点的恢复；1 GB Edge 长时间负载容量及来自目标用户地区的延迟比较。公开发行、单行安装、Ubuntu 26.04 Edge、重复安装和空队列卸载已实测，见下文。

上述第 7 项不能用本地 Docker 或模拟供应商结果代替。现有 `--update` 仍是原地升级；蓝绿是新增的显式转换与发布命令。首次转换涉及停止旧 SQLite 写入和 16002 入口交接，存在维护中断。不能回切到继续写 SQLite 的旧镜像，也不能通过恢复旧数据库快照进行版本回切。

## 同步主线与 Lightsail 调整

已合入主线 `6cf14fa` 的 YuAction 无状态 RAG 与知识状态轮询修复，保留新的 `rag_stateless_supported` 声明。合入后后端完整 race、带 event_worker 的 lint、依赖、原迁移部署升级/恢复检查再次通过。

Edge 支持 Ubuntu 24.04/26.04 引导安装，并检测缺少 Compose 的情况。Edge 发布清单独立于主站，候选要求 384 MiB 可用内存，应用容器每个颜色限 256 MiB。在 1 vCPU/256 MiB（禁用 swap）的 Edge 生产镜像中，回传队列崩溃恢复/独占锁与音频转发/网络分区/排空运行时测试通过（模拟主站与供应商也在测试进程内）。实机结果见下节。

## 2026-09-19 隔离实机与真实供应商验证

- 新加坡 EC2：Ubuntu 26.04、t3.medium、8 GiB 系统盘；伦敦、美西 Edge：Ubuntu 24.04、2 vCPU/1 GB。均为新建测试环境，没有连接现有生产数据库或应用卷。
- 主站首次转换、蓝绿切换和回切保留原测试数据库/应用卷及转换前后新增记录。实际重启曾暴露 SIGTERM 错误持久化排空状态的问题；修复后再次重启，入口、登录、记录及活动版本恢复正常。后续镜像仍需各自发布验收。
- 主站 PostgreSQL、应用文件、环境和发布状态的联合快照已在隔离容器/卷恢复，54 项迁移、两条测试记录及应用文件校验通过。未测试 R2 网络传输。
- 主站与两个 Edge 使用三个独立测试域名和 Tunnel。通过主站 API 自动创建节点 Tunnel/DNS，节点用一次性凭证注册；账户级 Cloudflare Token 未下发到 Edge。
- 实际 Cloudflare 拦截了 Python 默认 User-Agent，安装客户端改为 `DreamTrans-Edge/1.0`；全新目标目录未创建的问题也已修复。5 项安装引导/HTTP/首次目录测试通过。
- Edge 镜像通过校验和验证后导入两台机器，再运行镜像内安装控制器；这不是从公开发行仓库执行完整单行安装命令的验收。首次安装和空闲蓝绿升级均成功，升级前可用内存分别约 430/412 MiB；不能据此推算并发容量。
- Chromium 经公网 HTTPS 完成管理员登录并显示节点管理页，无 5xx 响应。
- 首轮真实 Speechmatics 测试发现空的 `AddTranscript` 阻塞结果队列与结算。新版 Edge 忽略空字幕；主站兼容旧 Edge 已入队的空字幕事件，保留有序用量但不创建空历史记录。旧队列无需删除或改写即可补传，20 条非空字幕保存成功，会话以 `completed` 结算。
- 修复后伦敦、美西分别发送约 12.9 秒合成英语音频，各接收 130 帧，20 条字幕全部从主站历史接口读取成功，无供应商错误。单次首字幕耗时约 1.45/1.58 秒，仅是测试客户端到各节点的单样本，不能当作地区 P50/P95 或优化收益证明。
- 本轮修复重新通过完整后端 race、event_worker race、lint、依赖验证、隔离数据库迁移、生命周期、真实镜像权限及迁移部署升级/故障恢复检查；Edge 256 MiB 运行时与 SIGTERM/重启检查通过。
- 约 51.6 秒真实音频期间，主站和伦敦 Edge 同时执行同镜像蓝绿发布：旧 Edge 在切流后保持 1 个 WebSocket，结束并确认回传后才停止；72 条非空字幕全部保存。此项验证实际排空机制，不代表小时级转录或所有跨版本组合已验证。
- 两台 Edge 实际重启后保持原身份、活动颜色及镜像，旧颜色仍停止，回传队列为零；重复安装保留完整配置。四个真实测试会话均正常关闭、预留全部结算，账本分钟数与接收音频样本数对应，原主站测试卷标识未变。
- 远端恢复部署测试暴露 PostgreSQL 初始化临时 Unix-socket 服务被误判就绪的问题。安装器和 Compose 改为检查最终 TCP 服务；恢复部署 fixture 增加初始化延迟，防止仅靠重跑掩盖启动竞态。

## 2026-09-19 协议 v2 故障验收

- 迁移 055 增加持久音频水位、恢复采样基线、明确的收费采样数及会话协议。旧会话保持 v1，旧事件哈希格式保持不变；新会话只能分配到支持其协议的节点。
- 独立数据库测试覆盖 12 个设备竞争两个节点、事务预留与并发限制、最终事件 8 次并发重试、乱序回传、过期回收、删除会话不能遗留预留、旧代次续租/写入拒绝，以及 v1/v2 滚动兼容。
- Edge 测试覆盖主站不可用时不建立付费供应商连接、网络分区持久队列、队列重开、持久化失败不推进确认水位、供应商输入流控和租约到期停止。256 MiB、1 CPU 容器内全部通过；active/standby/draining 三种模式的实际重启检查通过。
- 前端使用 Node 24.18.0，重新安装依赖与 Chromium，完整 `verify:ci` 的 59 项 E2E 全部首轮通过。此前与 Docker 生命周期并发执行的一次运行因本机接口变化导致 `ERR_NETWORK_CHANGED`，保留为失败记录，未计作通过。
- 伦敦 Edge 在真实 Speechmatics 转录中收到第 50 帧后被 SIGKILL；浏览器仅释放主站确认的前 17 帧。49.16 秒后租约失效，美西以代次 2 从第 18 帧重建供应商会话并继续，最终主站记录 20 条字幕。向主站重放旧节点事件返回 409。
- 两代账本合计收费 206441 个 16 kHz 采样，即整段 12.9025625 秒，预留全部释放；供应商已上报调用量为 238441 个采样，与用户收费量分开记录。崩溃前未回传的供应商消费无法推定，仍须供应商账单对账。
- 主站 Tunnel 暂停时，已建立的美西连接接受 300 帧、用满 30 秒预留后返回授权上限错误；没有 `EdgeSaved`。网络恢复后 37 条字幕补传，419200 个有最终确认的采样收费，未完成尾部释放。该测试暴露异常结束仍标记 completed 的可观测性问题，代码现区分 `interrupted_unfinalized_audio`，新增回归检查，完整后端 race、event_worker、lint 和依赖检查重新通过。
- 旧伦敦节点恢复后保留 2 条过时代次事件，进入隔离待对账状态；没有丢弃或将其写进新代字幕/账本。实际执行卸载被拒绝，2 条记录和运行容器均保留；随后恢复节点调度。归档处置继续验收，不能据此声称无人值守故障恢复已经全部完成。
- 所有主站升级沿用原测试 PostgreSQL 和应用卷；升级前新建包含数据库、应用卷、配置及状态的完整快照。生产数据与生产资源未操作。

## 后续生命周期、依赖与 R2 验收

- `877c929` 已推送，远端完整 CI [35473429792](https://github.com/CoYumeLabs/DreamTrans/actions/runs/35473429792) 已成功；后续提交的结果按各自流水线分别记录。
- 真实供应商 TCP 被重置后，原节点约 2.1 秒取得新代次，从主站确认的第 27 帧继续。两代共收费 206441 个采样，未用预留为零；异常首代记录为 `interrupted_unfinalized_audio`，新代正常完成。
- 测试主站安装 YuAction 固定版本 `5561118b4417b3bb5b8c42f790959d58f7e9cfd4`，使用同一 PostgreSQL 的 `yuaction` schema，`YUFOLO_URL=http://dreamtrans:8080` 指向固定入口。主站切流期间连续 80 次账户和房间读取成功，房间数据保留。此项不包含没有凭证的付费 AI 调用。
- 美西 Edge 的候选实例验证后被 SIGKILL：新增 `abort` 在只读检查队列为空且无进程占用后安全中止，当前入口不变。随后重试发布，在切流后的观察阶段 SIGKILL 控制器，重新执行 `resume` 成功恢复；同协议回切与排空成功，节点身份保留。
- 中止不能把失败候选当作上一版回切目标，也不能遗留“未验证空队列”的标志导致后续正常发布永久卡住。11 项发布恢复测试、7 项安装引导/目录/网络/队列检查通过；非空、损坏、占用中的队列拒绝中止复用。
- Edge 已卸载的审计目录不冒充有效安装。重新注册可使用新目录，但只复用带有本节点所有权标签的入口网络，未知同名网络拒绝接管。
- 修复蓝绿备份对数据库 Compose 服务名 `db` 及根目录 `release.py` 的假设；改用发布状态记录的实际数据库容器，并能从活动不可变镜像提取备份控制器。新增全量加密快照的本地保留数量检查。
- 使用独立临时 R2 bucket 和仅作用于该 bucket、24 小时有效的对象凭证，执行实际加密上传、下载、解密和清单验证。独立卷恢复后的 55 项迁移、12 个会话、275 条字幕、11 条 Edge 对账、13 条用量及 1 个 YuAction 房间与源库计数一致，应用文件也恢复到独立卷；故意篡改的归档成员被哈希检查发现。
- 实际 crontab 安装两次只有一个备份条目，已有条目保留；测试后恢复原 crontab。临时 R2 对象、bucket 和凭证在验收后清理，原有 bucket 不操作；加密本地证据和解密口令留在测试机受保护目录。
- 发行工作流增加独立 Edge 镜像的完整提交标签与主站/Edge digest 清单，沿用 CI 全部验证后才发布的门槛。Edge ARM64 二进制交叉编译通过；实机运行证据仍仅覆盖 amd64。


## 公开镜像、单行安装与跨地区重复样本

- `543bac8` 的[完整验证与镜像发布](https://github.com/CoYumeLabs/DreamTrans/actions/runs/35474422688)成功。主站与 Edge 均发布 amd64/arm64 镜像及固定 digest 清单，主站 `28e5515ba6322545fa12c6bf0472f4791c5cec2bbb838a4eaca52ac7df79a3c0`、Edge `a9fb3dba0a4b90fe18c532adb97a66cd8746e4bffbdd4eb202709bc3c08d0a49`，仓库均为 `ghcr.io/coyumelabs/dreamtrans`。测试环境按 digest 从公开仓库实际拉取，主站两个实际数据卷标识不变。
- 在 Ubuntu 26.04 的新 Edge 目录执行主站 API 生成的完整单行命令，自动校验引导脚本、拉取镜像、注册节点、配置独立 Tunnel 和启动定时控制器；仅追加受保护的注册/供应商凭证文件参数及并发上限。重复执行保留同一身份与配置，一次性注册凭证重放返回 401。该主机已有 Docker/Compose，不能据此声称自动安装系统依赖的分支已实机验证。
- 新加坡测试 Edge 与主站同机，但使用独立容器、入口网络、身份、域名与 Tunnel。它用于 Ubuntu 26.04 和同机部署基线验收，不是独立小规格节点容量测试。
- 首次公开安装因验收配置使用了导入镜像的本地 Nginx digest 而拉取失败；从官方 registry 获取实际 manifest digest 后重跑成功。失败发生在注册之前；保留失败记录。可发布 digest 不能从导入镜像的本地 ID 推定。
- 美西节点实际通过管理员设置目标 digest 升级；暂停自动发布时 `converge` 不切流，恢复后完成升级。队列排空后卸载成功，旧身份调用主站返回 401；轮换注册凭证后在新目录重装同一节点，复用本节点拥有的入口网络，旧审计目录保留，没有重复节点。实际重启后身份配置哈希、活动颜色、镜像和入口恢复一致。
- 实网安全边界：错误或缺失 Origin 的 Edge WebSocket 返回 403；伪造授权被断开；匿名管理、节点心跳、会话授权均返回 401。
- `f060539` 修复浏览器发送失败时两个缓冲区同时保留同一帧的问题：只有原生 WebSocket 接受帧后，Edge 恢复缓冲才取得该帧。回归检查经过真实 `SpeechmaticsProxyClient` 与区域传输适配器，验证重连后只发送一次。Node 24.18.0、依赖重装、Chromium 安装及完整 `verify:ci` 的 59 项 E2E 首轮通过；对应[远端完整 CI](https://github.com/CoYumeLabs/DreamTrans/actions/runs/35474825507)成功。这个修复晚于上述 `543bac8` 镜像，不能混称同一版本。

同一验收客户端经 Cloudflare WSS 向每区发送 8 次相同的 12.9025625 秒音频；共 24 个真实 Speechmatics 会话，字幕全部从主站历史读取，账本逐会话验证只计费一次且未结算预留为零。首次尝试同一 Free 账户并行测试被其 1 会话套餐上限拒绝，随后采用交替顺序样本，未改变套餐限制。以下是最终字幕而非临时字幕；持续延迟按收到时间减去音频起点及供应商片段结束时间计算，包含配置的 2 秒 `max_delay`。

| 节点 | 会话数 | 连接与授权 P50/P95（ms） | 首条最终字幕 P50/P95（ms） | 持续最终字幕 P50/P95（ms） |
|---|---:|---:|---:|---:|
| 新加坡，与主站同机 | 8 | 814 / 867 | 2759 / 2779 | 2071 / 2220 |
| 伦敦，1 GB Edge | 8 | 1161 / 1315 | 2770 / 2778 | 2082 / 2229 |
| 美西，1 GB Edge | 8 | 1332 / 1468 | 2790 / 2840 | 2124 / 2275 |

每区 8 次首字幕样本的 P95 实际为最大值，样本不足以估计生产尾延迟。这是同一客户端网络位置的实测，不代表目标用户浏览器的地域分布；结果不能证明远端 Edge 一定更快。仍需按实际用户网络做足量 A/B、长时间并发和供应商凭证隔离验收。伦敦两条旧代次事件继续保留待对账，没有通过删除队列来宣称发布已经可无人值守运行。


`f060539` 的[完整验证与公开镜像发布](https://github.com/CoYumeLabs/DreamTrans/actions/runs/35475178536)随后成功，包含上述浏览器恢复修复：主站 digest 为 `89049404157e6e4c271ecbce069e3eb87592bc4b5eb9f173cdcf3a10cdd493a6`，Edge 为 `71e59113a59c370a3e5b342313b5fd5b72fd57d91c5c10afce95ae9f0747c77b`。二者继续使用 `ghcr.io/coyumelabs/dreamtrans`。

美西 909 MiB 实机另外运行两路各 12.9 秒、两路各 51.6 秒的真实转录；长样本分别保存 70、71 条字幕，各计费 825764 个采样且预留全部释放。为了单独验证节点容量，隔离测试账户经管理 API 临时使用 3 会话套餐，结束后恢复 Free；第三路向 2 槽节点申请得到 409，没有绕过节点限制。256 MiB 容器的采样内存峰值为 22.31 MiB。首次长样本的一路在第 1 帧后断开，最终计费为零；后续两轮成功不能抹去首次故障，现有日志未能确定其来源，仍属待定位项。这些分钟级样本不构成持续负载容量保证。
